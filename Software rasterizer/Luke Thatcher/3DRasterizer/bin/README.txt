Both the Release and ReleaseSSE builds have
been provided as standalone executables.

The ReleaseSSE executable runs faster than
the regular Release build, but requires
hardware support for Intel SSE4 Instructions.

Unzip all files to the same folder before
running either application.